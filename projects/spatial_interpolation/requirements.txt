# Project-specific dependencies.
# Install the root requirements first:  pip install -r ../../requirements.txt
# (numpy, scipy, pandas, matplotlib, scikit-learn, pysindy, jupyter, tqdm come from there)

# Deep learning
torch

# CML / precipitation tooling
pynncml
pysteps

# Gridded data
xarray
netCDF4

# Experiment tracking
wandb
